<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_filter"
        android:title=""
        app:showAsAction="always"
        android:icon="@drawable/filter">
        <menu>

            <item
                android:id="@+id/menu_all"
                android:icon="@drawable/menu_all"
                android:title="Todos"/>

            <item
                android:id="@+id/menu_praga"
                android:icon="@drawable/menu_praga"
                android:title="Pragas"/>
            <item
                android:id="@+id/menu_temperatura"
                android:icon="@drawable/menu_temperatura"
                android:title="Temperatura"/>
            <item
                android:id="@+id/menu_umidade"
                android:icon="@drawable/menu_umidade"
                android:title="Umidade"/>

        </menu>
    </item>

    <item
        android:id="@+id/menu_watcher"
        android:title=""
        app:showAsAction="always"
        android:icon="@drawable/icon_watcher_white"
        />

</menu>